# E-Sparven
ESPARVEN_KEY=your_key_here
ESPARVEN_TEAM_ID=67

# GitHub (for Gist storage)
GITHUB_TOKEN=your_github_pat_here
GITHUB_USERNAME=Lundmarks

# Gist IDs (auto-filled after first run)
GIST_INDEX_ID=

# Optional: Discord webhook for notifications
DISCORD_WEBHOOK_URL=

# Optional: Discord role to @-mention on ready-check and match-day notifications
# (right-click the role in Server Settings -> Roles -> Copy Role ID, with
# Developer Mode enabled in User Settings -> Advanced)
DISCORD_ROLE_ID=

# Optional: Discord bot for scheduled-event creation (12 days out or closer).
# Different credential from the webhook above -- webhooks can't create
# scheduled events, only a bot with MANAGE_EVENTS in the target guild can.
# See discord_events.py's module docstring for full setup steps.
DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=

# Optional: comma-separated UTC times to run the bot each day (default: 2x per day)
RUN_TIMES=06:00,18:00
